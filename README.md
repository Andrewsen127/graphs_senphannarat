# graphs_senphannarat

A Python library for graph algorithms.  
Currently includes **Dijkstra’s shortest path algorithm**, using a min-heap implementation.

---

## Installation

Install the package directly from GitHub:

```bash
pip install git+https://github.com/Andrewsen127/graphs_senphannarat.git

